# ── DEV profile — plain HTTP for local development ───────────────────────────
# Access the app at: http://localhost:8080
# No certificate warnings. HTTPS is handled by Nginx in production only.

server.port=8080
server.address=0.0.0.0

# SSL disabled in dev — no certificate warnings
server.ssl.enabled=false
